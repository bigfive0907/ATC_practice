#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using P = pair<int,int>;

#define rep(i,a,n) for (int i = a; i < (n); ++i)

int main(){
  int n,m,l;
  cin >> n >> m >> l;
  cout << n*m*l << endl;
  return 0;
}